name: CICD_PROJECT
on:  
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: action/checkout@v4
      - uses: actions/setup-python@v5

  with:  
    python-version: '3.12'

    - run : python pythontest.py  
